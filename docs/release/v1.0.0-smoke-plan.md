# v1.0.0 — Post-deploy Smoke Test Plan

Execute only after explicit deployment approval.

## Public / auth
- `/` renders without console errors
- Sign-in flow reaches `/dashboard` and back to sign-out
- `/privacy`, `/terms`, `/risk`, `/release-notes`, `/status` render

## Core product
- Dashboard renders primary market data and does not crash on provider stale state
- `/options-chain` renders live chain when Upstox is up; degrades gracefully otherwise
- `/institutional-flow` renders analytics
- `/research-lab` and child routes load
- `/backtest-lab` renders and runs a canned strategy
- `/crypto` list and `/crypto/$pair` detail load via CoinDCX market data
- `/alerts` alert center loads
- `/ai-market-assistant` renders deterministic narrative

## Commercial / account
- `/pricing` renders manual-verification wording
- `/billing`, `/license`, `/profile`, `/settings` load for a signed-in user

## Admin
- `/admin/launch-readiness` renders and lists v1.0.0 stable gates
- `/admin/system-status` reports healthy or degraded per runtime evidence

## Cross-cutting
- No horizontal overflow at 320, 768, and 1440 CSS-pixel widths
- Keyboard focus reaches every primary nav item
- Trading-safety flags remain false in `/status` output
