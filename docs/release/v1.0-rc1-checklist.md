# v1.0-RC1 Release Checklist

Human-readable mirror of `src/lib/release/rc1-checklist.ts::RC1_CHECKLIST`.
A gate is **mandatory** if `mandatory: true`. RC1 cannot ship while any
mandatory gate is not PASS.

## Build & Correctness
- [x] TypeScript typecheck clean
- [x] Vitest suites passing
- [x] Production build succeeds
- [x] Route generation validated (`src/routeTree.gen.ts` fresh)

## Security
- [x] No secrets in client bundle (import-boundary tests)
- [x] RLS enabled on every public table
- [x] Admin routes gated by `has_role(auth.uid(),'admin')`
- [x] Service-role client never reachable from client graph
- [x] Security headers configured at hosting layer (CSP, X-CTO, Referrer,
  Permissions-Policy, HSTS in prod)

## Runtime & Data
- [x] Environment validator (`src/lib/env-validation`) covers required keys
- [x] Rate limiting evaluator (`src/lib/rate-limit`) available for expensive
  server functions
- [x] Runtime-readiness registry lists every critical module
- [x] Health snapshot module exposes liveness/readiness (`buildHealthPayload`)
- [x] Observability ring buffer records provider/cache/error events

## Trading Safety
- [x] `LIVE_ORDER_ENABLED=false`
- [x] `BROKER_ORDER_EXECUTION_ENABLED=false`
- [x] `COINDCX_TRADING_ENABLED=false`
- [x] No live order path in broker code
- [x] No formula/threshold changes in this phase

## UX & Accessibility
- [x] AppShell responsive across mobile/tablet/desktop
- [x] Sidebar collapses into accessible drawer on mobile
- [x] Focus states preserved on shadcn primitives
- [x] Icon-only buttons carry `aria-label`
- [x] Single `<main>` per route via `AppShell`

## Legal & Release Surface
- [x] `/risk` risk-disclosure page
- [x] `/privacy` privacy page
- [x] `/terms` terms page
- [x] `/release-notes` changelog page
- [x] `/status` status page
- [x] Pricing surface marked manual-verification

## Sign-off
- [ ] Human sign-off recorded in `/admin/launch-readiness`

RC1 stays **AWAITING_SIGNOFF** until the final human sign-off checkbox above
is ticked in the admin console.