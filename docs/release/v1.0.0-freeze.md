# EagleBABA v1.0.0 — Release Freeze

_Phase 4B — release execution._

## Frozen build identity
- Version: **1.0.0**
- Channel: **stable**
- Prior baseline: `v1.0-rc1` (see `docs/release/v1.0-rc1-baseline.md`)
- Build timestamp: emitted by CI at deploy time (see `V1_MANIFEST.buildTimestamp`)
- Commit SHA / build ID: injected by CI; `null` in this repo snapshot
- Environment schema: `src/lib/env-validation/index.ts::DEFAULT_ENV_REQUIREMENTS`
- Route source of truth: `src/routeTree.gen.ts` (auto-generated)
- Migration state: no destructive migrations required for v1.0.0
- Runtime module registry: `src/lib/runtime-readiness/runtime-evidence.ts`
- Formula versions: `astro@1.0.0`, `gann@1.0.0`, `gti@1.0.0`, `decision@1.0.0`
- Provider adapters: Upstox (broker + market data, read-only), CoinDCX (market data only)

## Frozen flag state
- `LIVE_ORDER_ENABLED=false`
- `BROKER_ORDER_EXECUTION_ENABLED=false`
- `COINDCX_TRADING_ENABLED=false`

Any change to these flags after freeze is a release event and re-opens the
stable gate.

## Immutability rule
The frozen build is immutable except for release-blocking fixes recorded
in the changelog and re-evaluated through `evaluateV1StableReadiness()`.
